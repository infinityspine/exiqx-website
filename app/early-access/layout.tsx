import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Early Access | EXIQX™ Performance',
  description:
    'Get early access to EXIQX™ Nordic Forefoot Trainer systems. Limited production runs available for elite training facilities.',
}

export default function EarlyAccessLayout({ children }: { children: React.ReactNode }) {
  return children
}

